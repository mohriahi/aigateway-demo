# ==============================================================================
# SHARED WSO2 GATEWAY CREDENTIALS (Required - Global Fallback)
# ==============================================================================
# These credentials are used by all applications that don't have specific credentials
# Get these from your WSO2 Developer Portal by creating an application and generating keys

WSO2_CONSUMER_KEY=your_shared_consumer_key_here
WSO2_CONSUMER_SECRET=your_shared_consumer_secret_here
WSO2_TOKEN_URL=https://your-wso2-server:9443/oauth2/token

# ==============================================================================
# PROVIDER-SPECIFIC ENDPOINTS (Required)
# ==============================================================================
# These URLs come from the API subscriptions in your WSO2 Developer Portal
# Subscribe your WSO2 application to each LLM API to get these endpoints
# Pattern: {PROVIDER_NAME}_CHAT_COMPLETIONS_URL (must match provider names in config.yaml)
# Note: For Gemini, you can use {model} placeholder or include the model name directly.
# Note 2: TODO We will have to rename CHAT_COMPLETIONS_URL to be consistent with all providers

OPENLLM_CHAT_COMPLETIONS_URL=https://your-wso2-server:8243/openaiapi/v1/chat/completions
OPENAI_CHAT_COMPLETIONS_URL=https://your-wso2-server:8243/openaiapi/v1/chat/completions
MISTRAL_CHAT_COMPLETIONS_URL=https://your-wso2-server:8243/mistralapi/v1/chat/completions
MISTRAL2_CHAT_COMPLETIONS_URL=https://your-wso2-server:8243/mistralapi/v1/chat/completions
ANTHROPIC_CHAT_COMPLETIONS_URL=https://your-wso2-server:8243/anthropicapi/v1/messages
ANTHROPIC_CLOUD_CHAT_COMPLETIONS_URL=https://your-wso2-server:8243/anthropicapi/v1/messages
GEMINI_CHAT_COMPLETIONS_URL=https://your-wso2-server:8243/geminiapi/v1beta/v1beta/models/{model}:generateContent


# ==============================================================================
# APPLICATION-SPECIFIC OAUTH CREDENTIALS (Optional)
# ==============================================================================
# Each application can have its own OAuth credentials for separate statistics tracking
# If not specified, applications will use the shared WSO2_* credentials above
# Pattern: {APPLICATION_KEY}_CONSUMER_KEY (must match application key in applications.yaml)

# Example: Mobile Application
# MOBILE_CONSUMER_KEY=mobile_app_consumer_key_here
# MOBILE_CONSUMER_SECRET=mobile_app_consumer_secret_here
# MOBILE_TOKEN_URL=https://your-wso2-server:9443/oauth2/token

# Example: Web Application
# WEBAPP_CONSUMER_KEY=webapp_consumer_key_here
# WEBAPP_CONSUMER_SECRET=webapp_consumer_secret_here
# WEBAPP_TOKEN_URL=https://your-wso2-server:9443/oauth2/token

# ==============================================================================
# IMPORTANT NOTES
# ==============================================================================
# 1. The APPLICATION_KEY in environment variables must match the key in applications.yaml (uppercased)
#    Example: "mobile" in applications.yaml → MOBILE_CONSUMER_KEY in .env
#
# 2. The PROVIDER_NAME in endpoint URLs must match the provider key in config.yaml (uppercased)
#    Example: "MISTRAL" in config.yaml → MISTRAL_CHAT_COMPLETIONS_URL in .env
#
# 3. All provider endpoint URLs come from your WSO2 Developer Portal API subscriptions
#
# 4. If an application doesn't have specific credentials, it will use the shared WSO2_* credentials
#
# 5. At minimum, you MUST configure:
#    - WSO2_CONSUMER_KEY, WSO2_CONSUMER_SECRET, WSO2_TOKEN_URL (shared credentials)
#    - At least one provider endpoint URL (e.g., OPENLLM_CHAT_COMPLETIONS_URL)
